extends ../base
include ../mixins/equip

block content
    div.profile__data
        img(src="/" + user.avatarUrl).avatar
        h3=user.name
    div.equip-grid
        each equip in user.equips
            +equipsmixin(equip)
        else 
            span.empty__message No Equips found